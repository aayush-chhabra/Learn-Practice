#include <stdio.h>

int computeRainForCuboid(int height){

}

void rain(int m, int *heights, int heights_length) {
   	totalWidth = heights_length / m;
   	 
}

int main(){
	int heights[] = {3, 3, 4, 4, 4, 2, 3, 1, 3, 2, 1, 4, 7, 3, 1, 6, 4, 1};
	int heights_length = sizeof(heights)/sizeof(int);
	rain(6, heights, heights_length);
}